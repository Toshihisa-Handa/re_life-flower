<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>花一覧ページ</title>
    <link rel="stylesheet" href="css/style.css">
    <style>
         .flower-container{
            display: flex;
            flex-wrap: wrap;
        }
        .flower-card{
            margin-top: 50px;
            margin-right: 50px;
            background-color: rgb(247, 162, 235);
            width: 300px;
        }
        .flower-card:hover{
            opacity:0.7;
        }
    </style>
</head>
<body>
    
    <header>
        <ul>
          <li><a href="/">ホーム</a></li>
          <li><a href="/shops">店舗</a></li>
          <li><a href="/diarys">日記</a></li>
          <li><a href="/flowers">お花</a></li>
          <li><a href="">検索</a></li>
          <li><a href="/login">ログイン</a></li>
        </ul>
     </header>
    <h1>花一覧ページ</h1>
    <% if (typeof user !== 'undefined') { %>
        <span class="login-user"><%= user.name %>さんとしてログインしています</span>
    <% } %>


   
    <div class="flower-container">
    <% items.forEach((item) => { %>
        <div class='flower-card'><a href="/flower/<%=item.id%>">
       
            <h2><%= item.name%></h2>
            <h2><%= item.price%>円</h2>
            <h2><%= item.feature%></h2>
            <p><%= item.tag%></p>
            <h3><%= item.text%></h3>
            <p><%= item.created_at%></p>
           <img src="images/uploads/<%= item.image %>" alt="" style='width:300px'>
           <h2><%= item.shopname %></h2>
        </a></div>
       <% }) %>
    </div>
</body>
</html>